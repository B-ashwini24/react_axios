const Unmounting=()=>{

    return (
        <div style={{textAlign:'center'}}>
            <h1>Unmounting</h1>
            <h2>
            This is the final phase of the lifeycle of the component<br/>
             that is the phase of unmounting the component from the DOM<br/>
             In component lifecycle unmounting is excecuted with useEffect with return function<br/>
                   

            </h2>
        </div>
    )
}

export default Unmounting;